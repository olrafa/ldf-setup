'use strict';

/**
 * record-page-description service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::record-page-description.record-page-description');
