'use strict';

/**
 * record-page-description router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::record-page-description.record-page-description');
