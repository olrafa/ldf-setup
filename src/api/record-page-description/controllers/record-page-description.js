'use strict';

/**
 * record-page-description controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::record-page-description.record-page-description');
